---
title: "431 Request Header Fields Too Large"
description: "Either an individual header or all headers collectively are too huge. Often caused by having a massive amount of cookies."
---
import { Tabs, TabItem } from '@astrojs/starlight/components';

Either an individual header or all headers collectively are too huge. Often caused by having a massive amount of cookies.

## When and Why to Use It
Use this if the client is sending cookies so massive that they threaten to crash your header parser.

### Usage Examples

<Tabs>
  <TabItem label="Shokupan (Functional)">

```typescript
import { HttpException, get } from 'shokupan';

export const getResource = get('/resource', () => {
    throw new HttpException('Request Header Fields Too Large', 431);
});
```

  </TabItem>
  <TabItem label="Shokupan (Controller)">

```typescript
import { Controller, Get, HttpException } from 'shokupan';

@Controller('/api')
export class ExampleController {
    @Get('/resource')
    getResource() {
        throw new HttpException('Request Header Fields Too Large', 431);
    }
}
```

  </TabItem>
  <TabItem label="Express">

```typescript
import express from 'express';
const app = express();

app.get('/resource', (req, res) => {
    res.status(431).json({ error: 'Request Header Fields Too Large' });
});
```

  </TabItem>
  <TabItem label="Nest">

```typescript
import { Controller, Get, HttpException } from '@nestjs/common';

@Controller('api')
export class ExampleController {
  @Get('resource')
  getResource() {
    throw new HttpException('Request Header Fields Too Large', 431);
  }
}
```

  </TabItem>
  <TabItem label="Koa">

```typescript
import Koa from 'koa';
const app = new Koa();

app.use(async ctx => {
  if (ctx.path === '/resource' && ctx.method === 'GET') {
    ctx.status = 431;
    ctx.body = { error: 'Request Header Fields Too Large' };
  }
});
```

  </TabItem>
</Tabs>
